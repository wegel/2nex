package:
  schema: 1
  name: bmon
  slug: bmon
  namespace: cli/system
  version: "4.0"
  description: Bandwidth monitor and rate estimator
  homepage: https://github.com/tgraf/bmon
  checksum: f1cd5790ce49afde4d00164f62ad18ead23b73540ac085c042e71be6f11661ac

sources:
- name: bmon
  url: https://github.com/tgraf/bmon/releases/download/v4.0/bmon-4.0.tar.gz
  sha256: 02fdc312b8ceeb5786b28bf905f54328f414040ff42f45c83007f24b76cc9f7a
- name: uapi_config
  file: pkg/cli/system/bmon-uapi-config.patch
  sha256: e8586c2c143c189ad815b090e4a7b6474b20bab7b7a048ff7962b75efaf6b062

dependencies:
- name: bash
  commit: x86_64/pkg/cli/shells/bash/5.2.21/bundles/dev
- name: coreutils
  commit: x86_64/pkg/core/userland/coreutils/9.5/bundles/dev
- name: findutils
  commit: x86_64/pkg/core/userland/findutils/4.9.0/bundles/dev
- name: tar
  commit: x86_64/pkg/cli/archive/tar/1.35/bundles/dev
- name: gzip
  commit: x86_64/pkg/cli/archive/gzip/1.13/bundles/dev
- name: glibc
  commit: x86_64/pkg/libs/system/glibc/2.39/bundles/dev
- name: gcc
  commit: x86_64/pkg/core/toolchain/gcc/13.2.0/bundles/dev
- name: binutils
  commit: x86_64/pkg/core/toolchain/binutils/2.42/bundles/dev
- name: make
  commit: x86_64/pkg/core/toolchain/make/4.4.1/bundles/dev
- name: pkgconf
  commit: x86_64/pkg/core/toolchain/pkgconf/2.2.0/bundles/dev
- name: sed
  commit: x86_64/pkg/cli/text/sed/4.9/bundles/dev
- name: gawk
  commit: x86_64/pkg/cli/text/gawk/5.3.0/bundles/dev
- name: grep
  commit: x86_64/pkg/cli/text/grep/3.11/bundles/dev
- name: diffutils
  commit: x86_64/pkg/core/userland/diffutils/3.10/bundles/dev
- name: file
  commit: x86_64/pkg/dev/tools/file/5.45/bundles/dev
- name: linux-headers
  commit: x86_64/pkg/libs/system/linux-headers/6.4.1/bundles/dev
- name: ncurses
  commit: x86_64/pkg/libs/system/ncurses/6.4-20230520/bundles/dev
- name: libconfuse
  commit: x86_64/pkg/libs/data/libconfuse/3.3/bundles/dev
- name: libnl
  commit: x86_64/pkg/libs/net/libnl/3.11.0/bundles/dev
- name: acl
  commit: x86_64/pkg/libs/system/acl/2.3.2/bundles/dev
- name: attr
  commit: x86_64/pkg/libs/system/attr/2.5.2/bundles/dev
- name: libcap
  commit: x86_64/pkg/libs/security/libcap/2.69/bundles/dev
- name: zlib
  commit: x86_64/pkg/libs/system/zlib/1.3.1/bundles/dev
- name: gmp
  commit: x86_64/pkg/libs/system/gmp/6.3.0/bundles/dev
- name: patch
  commit: x86_64/pkg/core/toolchain/patch/2.7.6/bundles/dev

build:
  environment: 27b6e5dc7ad152c9a17c2cabfcc5ee93daa9bbf0
  profile: [4115:2729, 8220:6322, 8641:6359]

  script: |
    tar --no-same-owner -xf ${SOURCE_bmon} -C "${WORK_DIR}" --strip-components=1
    cd ${WORK_DIR}

    patch --batch --fuzz=0 -Np1 -i "/${SOURCE_uapi_config}"

    ./configure --prefix=/usr --sysconfdir=/etc

    make
    make DESTDIR="${OUT_DIR}" install

    # configfile_read() overlays each system file it can read, later files
    # winning: /usr/lib/bmon/bmon.conf, then /run/bmon.conf, then
    # /etc/bmon.conf, then ~/.bmonrc. conf_read() prints the file path on a
    # parse error, which is enough to prove each tier was opened in order.
    mkdir -p /etc /run /usr/lib/bmon
    export HOME="${WORK_DIR}/bmon-empty-home"
    mkdir -p "${HOME}"

    vendor=/usr/lib/bmon/bmon.conf
    runtime=/run/bmon.conf
    admin=/etc/bmon.conf
    rm -f "${vendor}" "${runtime}" "${admin}"

    expect_error_names() {
      if out=$("${OUT_DIR}/usr/bin/bmon" -o ascii:quitafter=1 2>&1); then
        printf 'bmon UAPI smoke: expected nonzero exit reading %s\n' "$1" >&2
        printf '%s\n' "${out}" >&2
        exit 1
      fi
      case "${out}" in
        *"$1"*) ;;
        *)
          printf 'bmon UAPI smoke: expected %s named in error\n' "$1" >&2
          printf '%s\n' "${out}" >&2
          exit 1
          ;;
      esac
    }

    printf 'this is not a valid bmon config\n' > "${vendor}"
    expect_error_names "${vendor}"

    printf 'show_all = true\n' > "${vendor}"
    printf 'this is not a valid bmon config\n' > "${runtime}"
    expect_error_names "${runtime}"

    printf 'show_all = true\n' > "${runtime}"
    printf 'this is not a valid bmon config\n' > "${admin}"
    expect_error_names "${admin}"

    printf 'show_all = true\n' > "${admin}"
    if ! "${OUT_DIR}/usr/bin/bmon" -o ascii:quitafter=1 >/tmp/bmon-uapi-ok 2>&1; then
      printf 'bmon UAPI smoke: expected a clean run with all three tiers valid\n' >&2
      cat /tmp/bmon-uapi-ok >&2
      exit 1
    fi

    rm -f "${vendor}" "${runtime}" "${admin}"
    printf 'bmon UAPI smoke: the config loader read all three tiers in order\n'

    find ${OUT_DIR} -type f -executable -exec strip --strip-unneeded {} \; 2>/dev/null || true
    find ${OUT_DIR} -name '*.a' -exec objcopy --enable-deterministic-archives {} \; 2>/dev/null || true
    find "${OUT_DIR}" -exec touch -d "2024-01-01T00:00:00+00:00" {} \;

bundles:
  dev:
  - bin
  - man

outputs:
  bin:
    files:
    - path: /usr/bin/bmon
      needs:
      - /usr/lib/ld-linux-x86-64.so.2
      - /usr/lib/libc.so.6
      - /usr/lib/libconfuse.so.2
      - /usr/lib/libm.so.6
      - /usr/lib/libncursesw.so.6
      - /usr/lib/libnl-3.so.200
      - /usr/lib/libnl-route-3.so.200

  doc:
    files:
    - path: /usr/share/doc/bmon/examples/bmon.conf

  man:
    files:
    - path: /usr/share/man/man8/bmon.8

resolution:
  /usr/lib/ld-linux-x86-64.so.2: glibc
  /usr/lib/libc.so.6: glibc
  /usr/lib/libm.so.6: glibc
  /usr/lib/libconfuse.so.2: libconfuse
  /usr/lib/libnl-3.so.200: libnl
  /usr/lib/libnl-route-3.so.200: libnl
  /usr/lib/libncursesw.so.6: ncurses
