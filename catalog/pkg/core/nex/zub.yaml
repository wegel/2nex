package:
  schema: 1
  name: zub
  slug: zub
  namespace: core/nex
  version: 0.0.3-17f2ca6
  description: Git-like content-addressed filesystem store used by Nex
  homepage: https://github.com/wegel/zub
  checksum: 5b129ef9e47b15ba25885f86edc72e515bdd33d9609efc62d53a2e2b60a8b5ae

sources:
- name: zub
  url: https://github.com/wegel/zub/archive/17f2ca6d1437a32e4123a9a30ac47a3896254a55.tar.gz
  sha256: 99d80f49445ae370ca5bf4e1f262fe9feaee1dde82494e7e078e02b3fcb3d761
- name: vendor
  sha256: 0e2853ae62391172ff1786df7db9cbead20277c8bc788890089db477d2b24033
  cargo_lock: https://raw.githubusercontent.com/wegel/zub/17f2ca6d1437a32e4123a9a30ac47a3896254a55/Cargo.lock

dependencies:
- name: bash
  commit: x86_64/pkg/cli/shells/bash/5.2.21/bundles/dev
- name: coreutils
  commit: x86_64/pkg/core/userland/coreutils/9.5/bundles/dev
- name: findutils
  commit: x86_64/pkg/core/userland/findutils/4.9.0/bundles/dev
- name: gzip
  commit: x86_64/pkg/cli/archive/gzip/1.13/bundles/dev
- name: tar
  commit: x86_64/pkg/cli/archive/tar/1.35/bundles/dev
- name: binutils
  commit: x86_64/pkg/core/toolchain/binutils/2.42/bundles/dev
- name: glibc
  commit: x86_64/pkg/libs/system/glibc/2.39/bundles/dev
- name: gcc
  commit: x86_64/pkg/core/toolchain/gcc/13.2.0/bundles/dev
- name: rust-bin
  commit: x86_64/pkg/dev/lang/rust-bin/1.91.1/bundles/dev
- name: linux-headers
  commit: x86_64/pkg/libs/system/linux-headers/6.4.1/bundles/dev
- name: ncurses
  commit: x86_64/pkg/libs/system/ncurses/6.4-20230520/bundles/dev
- name: acl
  commit: x86_64/pkg/libs/system/acl/2.3.2/bundles/dev
- name: attr
  commit: x86_64/pkg/libs/system/attr/2.5.2/bundles/dev
- name: zlib
  commit: x86_64/pkg/libs/system/zlib/1.3.1/bundles/dev

build:
  environment: 248c5014338ab55a89b9c261e303e111e6847aec
  profile: [2248:11758]

  script: |
    set -eu

    CFLAGS=${CFLAGS/-flto=auto/}
    CXXFLAGS=${CXXFLAGS/-flto=auto/}
    LDFLAGS=${LDFLAGS/-flto=auto/}

    tar --no-same-owner -xf "${SOURCE_zub}" -C "${WORK_DIR}" --strip-components=1
    cd "${WORK_DIR}"

    tar --no-same-owner -xf "/./${SOURCE_vendor}" -C "${WORK_DIR}"

    export CARGO_HOME="${WORK_DIR}/.cargo"
    mkdir -p "${CARGO_HOME}"
    cat > "${CARGO_HOME}/config.toml" << EOF
    [source.crates-io]
    replace-with = "vendored-sources"

    [source.vendored-sources]
    directory = "vendor"
    EOF

    mkdir -p /usr/local/bin
    ln -sf /usr/bin/gcc /usr/local/bin/cc
    ln -sf /usr/bin/ld.bfd /usr/local/bin/ld
    export PATH="/usr/local/bin:${PATH}"
    export CARGO_TARGET_X86_64_UNKNOWN_LINUX_GNU_LINKER=gcc
    unset RUSTFLAGS

    cargo build --release --offline --locked

    mkdir -p "${OUT_DIR}/usr/bin"
    install -m 755 target/release/zub "${OUT_DIR}/usr/bin/zub"

    strip --strip-unneeded "${OUT_DIR}/usr/bin/zub"
    find "${OUT_DIR}" -exec touch -d '2024-01-01T00:00:00+00:00' {} \;

bundles:
  dev:
  - bin

  full:
  - bin

outputs:
  bin:
    files:
    - path: /usr/bin/zub
      needs:
      - /usr/lib/ld-linux-x86-64.so.2
      - /usr/lib/libc.so.6
      - /usr/lib/libgcc_s.so.1

resolution:
  /usr/lib/libgcc_s.so.1: gcc
  /usr/lib/ld-linux-x86-64.so.2: glibc
  /usr/lib/libc.so.6: glibc
